[project]
name = "offboarding-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=4.5.0",
    "dotenv>=0.9.9",
    "path>=17.1.1",
    "torch>=2.10.0",
    "transformers>=5.1.0",
]
